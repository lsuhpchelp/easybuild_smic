easyblock = "Tarball"

name = 'vmatch'
version = '2.3.0'
author = 'lyan1'
requested_by = 'bio'

homepage = 'http://www.vmatch.de/'
description = """ Vmatch is a versatile software tool for efficiently solving large scale sequence matching tasks."""

toolchain = {'name': 'dummy', 'version': 'dummy'}

source_urls = ['http://vmatch.de/distributions/']
sources = ['%(name)s-%(version)s-Linux_x86_64-64bit.tar.gz']

sanity_check_paths = {
    'files': ['vmatch'],
    'dirs': [],
}

modextrapaths = {'PATH':''}

